version: '3.8'

services:
  homevault:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: homevault
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      LOG_LEVEL: info
      # Base de datos SQLite persistente
      DATABASE_URL: file:/data/homevault.db
      JWT_SECRET: ${JWT_SECRET:-change-me-in-production}
      # Configuración del servidor
      API_CORS_ORIGIN: ${API_CORS_ORIGIN:-*}
      STORAGE_BASE_PATH: ${STORAGE_BASE_PATH:-/opt/homevault/data}
    volumes:
      # Persistencia de base de datos
      - ./data:/data
      # Persistencia de logs
      - ./logs:/app/logs
      # Acceso a rutas de almacenamiento (si existen en el host)
      - /opt/homevault/data:/opt/homevault/data:ro
      # Socket de Docker (para gestión de contenedores)
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - homevault-network
    # Límites de recursos (ajusta según tu hardware)
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 512M
        reservations:
          cpus: '1'
          memory: 256M

  # Base de datos backup (opcional)
  # Sincroniza la base de datos cada 6 horas
  backup-cron:
    image: alpine:latest
    container_name: homevault-db-backup
    restart: unless-stopped
    volumes:
      - ./data:/data
      - ./backups:/backups
    entrypoint: |
      sh -c '
      while true; do
        echo "Realizando backup de base de datos..."
        cp /data/homevault.db /backups/homevault-$(date +%s).db.bak
        # Mantener solo los últimos 10 backups
        ls -1 /backups/homevault-*.db.bak | sort -r | tail -n +11 | xargs -r rm
        echo "Backup completado"
        sleep 21600
      done
      '
    networks:
      - homevault-network

networks:
  homevault-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.25.0.0/16

volumes:
  homevault-data:
    driver: local
